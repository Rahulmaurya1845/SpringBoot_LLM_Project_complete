server:
  port: 8081

spring:
  application:
    name: ai-agent-backend

  ai:
    openai:
      api-key: ${OPENAI_API_KEY:default-key}
      base-url: https://api.groq.com/openai/v1
      chat:
        model: ${CHAT_MODEL:openai/gpt-oss-20b}